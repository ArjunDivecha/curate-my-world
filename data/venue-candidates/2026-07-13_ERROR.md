# SVDA run rejected — 2026-07-13

This run is retained as a failure audit only. It produced no approved candidate
artifact and proposes no changes to `data/venue-registry.json`.

## Validation failures

- The run used a legacy Claude Code Routine instead of the guarded Managed
  Agents workflow in `.github/workflows/svda-managed-agent.yml`.
- It ran for approximately 25 minutes, exceeding the seven-minute limit.
- It researched eight candidates, exceeding the five-candidate limit.
- Every direct event-page fetch returned HTTP 403. Search-result snippets are
  not a substitute for the required in-session page fetch.
- The proposed Carnatic Chamber Concerts row treated roughly 12 events per year
  as 12 events per month. The supported estimate is approximately one event per
  month, below the three-events-per-month registry cutoff.
- The proposed India Community Center row inferred five events per month from
  roughly six events over five months. That evidence supports approximately one
  event per month, also below the registry cutoff.
- The generated branch was 14 commits behind `main` and rewrote existing
  registry text while serializing JSON, creating unrelated changes and a merge
  conflict.

## Disposition

The candidate claims are not promoted to the JSON sidecar or production
registry. A future run may reconsider them only after directly fetching the
event pages and producing evidence that satisfies the current volume and
validation rules.

Original Claude Code session reference:
`session_01GB7V4iVKPtq5K6rNkyCpgH` (no longer accessible from the configured
Claude workspace).
